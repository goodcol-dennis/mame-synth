[package]
name = "synth-core"
version = "0.1.0"
edition = "2021"
build = "build.rs"

[dependencies]
cpal = "0.15"
midir = "0.10"
rtrb = "0.3"
log = { workspace = true }
anyhow = "1"

[dev-dependencies]
proptest = "1"

[build-dependencies]
cc = { version = "1.0", features = ["parallel"] }
